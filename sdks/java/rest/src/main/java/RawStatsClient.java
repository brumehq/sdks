/**
 * This file was auto-generated by Fern from our API Definition.
 */

package dev.brume;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import dev.brume.core.BrumeClientApiException;
import dev.brume.core.BrumeClientException;
import dev.brume.core.BrumeClientHttpResponse;
import dev.brume.core.ClientOptions;
import dev.brume.core.ObjectMappers;
import dev.brume.core.QueryStringMapper;
import dev.brume.core.RequestOptions;
import dev.brume.errors.ForbiddenError;
import dev.brume.errors.UnauthorizedError;
import dev.brume.types.AnalyticsRequest;
import dev.brume.types.ErrorBody;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import java.util.Map;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawStatsClient {
  protected final ClientOptions clientOptions;

  public RawStatsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Returns the project's analytics history as a list of snapshots
   * (oldest first). Snapshots are sampled every 30s in-process. The
   * response includes the last <code>window / interval</code> entries. Empty
   * state is an empty <code>snapshots</code> array — never faked data.
   * <p>Response body is documented as <code>additionalProperties: true</code> for
   * the same reason as <code>/v1/stats</code>: the shape is pinned by tests.</p>
   */
  public BrumeClientHttpResponse<Map<String, Object>> analytics() {
    return analytics(AnalyticsRequest.builder().build());
  }

  /**
   * Returns the project's analytics history as a list of snapshots
   * (oldest first). Snapshots are sampled every 30s in-process. The
   * response includes the last <code>window / interval</code> entries. Empty
   * state is an empty <code>snapshots</code> array — never faked data.
   * <p>Response body is documented as <code>additionalProperties: true</code> for
   * the same reason as <code>/v1/stats</code>: the shape is pinned by tests.</p>
   */
  public BrumeClientHttpResponse<Map<String, Object>> analytics(RequestOptions requestOptions) {
    return analytics(AnalyticsRequest.builder().build(),requestOptions);
  }

  /**
   * Returns the project's analytics history as a list of snapshots
   * (oldest first). Snapshots are sampled every 30s in-process. The
   * response includes the last <code>window / interval</code> entries. Empty
   * state is an empty <code>snapshots</code> array — never faked data.
   * <p>Response body is documented as <code>additionalProperties: true</code> for
   * the same reason as <code>/v1/stats</code>: the shape is pinned by tests.</p>
   */
  public BrumeClientHttpResponse<Map<String, Object>> analytics(AnalyticsRequest request) {
    return analytics(request,null);
  }

  /**
   * Returns the project's analytics history as a list of snapshots
   * (oldest first). Snapshots are sampled every 30s in-process. The
   * response includes the last <code>window / interval</code> entries. Empty
   * state is an empty <code>snapshots</code> array — never faked data.
   * <p>Response body is documented as <code>additionalProperties: true</code> for
   * the same reason as <code>/v1/stats</code>: the shape is pinned by tests.</p>
   */
  public BrumeClientHttpResponse<Map<String, Object>> analytics(AnalyticsRequest request,
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("v1/analytics");if (request.getWindowSecs().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "window_secs", request.getWindowSecs().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new BrumeClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, new TypeReference<Map<String, Object>>() {}), response);
        }
        try {
          switch (response.code()) {
            case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorBody.class), response);
            case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorBody.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new BrumeClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new BrumeClientException("Network error executing HTTP request", e);
      }
    }

    /**
     * Returns project-level and global connection/channel statistics.
     * <p>Backwards compatibility: existing fields are preserved exactly. New
     * fields added in 2026-06-11 (latency, postgres_lag, dropped_messages,
     * slow_consumer_disconnections, dead_connections_cleaned,
     * auth_failures_last_minute, plan_limit_rejections, top_channels) are
     * additive — older dashboard clients keep working.</p>
     * <p>Response body is documented as <code>additionalProperties: true</code> because
     * the exact JSON shape is pinned by ~10 unit tests in this file. A
     * future PR can type the envelope; for now SDK consumers will see
     * <code>Record&lt;string, any&gt;</code>.</p>
     */
    public BrumeClientHttpResponse<Map<String, Object>> getStats() {
      return getStats(null);
    }

    /**
     * Returns project-level and global connection/channel statistics.
     * <p>Backwards compatibility: existing fields are preserved exactly. New
     * fields added in 2026-06-11 (latency, postgres_lag, dropped_messages,
     * slow_consumer_disconnections, dead_connections_cleaned,
     * auth_failures_last_minute, plan_limit_rejections, top_channels) are
     * additive — older dashboard clients keep working.</p>
     * <p>Response body is documented as <code>additionalProperties: true</code> because
     * the exact JSON shape is pinned by ~10 unit tests in this file. A
     * future PR can type the envelope; for now SDK consumers will see
     * <code>Record&lt;string, any&gt;</code>.</p>
     */
    public BrumeClientHttpResponse<Map<String, Object>> getStats(RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("v1/stats");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("GET", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new BrumeClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, new TypeReference<Map<String, Object>>() {}), response);
          }
          try {
            switch (response.code()) {
              case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorBody.class), response);
              case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorBody.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new BrumeClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new BrumeClientException("Network error executing HTTP request", e);
        }
      }
    }
