/**
 * This file was auto-generated by Fern from our API Definition.
 */

package dev.brume.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import dev.brume.core.Nullable;
import dev.brume.core.NullableNonemptyFilter;
import dev.brume.core.ObjectMappers;
import java.lang.Integer;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = PlanLimitErrorBody.Builder.class
)
public final class PlanLimitErrorBody {
  private final String code;

  private final int current;

  private final Optional<Integer> limit;

  private final String message;

  private final String reason;

  private final Optional<String> upgradeTier;

  private final Map<String, Object> additionalProperties;

  private PlanLimitErrorBody(String code, int current, Optional<Integer> limit, String message,
      String reason, Optional<String> upgradeTier, Map<String, Object> additionalProperties) {
    this.code = code;
    this.current = current;
    this.limit = limit;
    this.message = message;
    this.reason = reason;
    this.upgradeTier = upgradeTier;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return Always <code>&quot;PLAN_LIMIT&quot;</code>.
   */
  @JsonProperty("code")
  public String getCode() {
    return code;
  }

  /**
   * @return Current count for the axis that triggered the limit.
   */
  @JsonProperty("current")
  public int getCurrent() {
    return current;
  }

  /**
   * @return The cap value, or <code>null</code> for unlimited (e.g. business tier
   * connection cap).
   */
  @JsonIgnore
  public Optional<Integer> getLimit() {
    if (limit == null) {
      return Optional.empty();
    }
    return limit;
  }

  /**
   * @return Human-readable message. Includes the current/limit numbers
   * and the upgrade suggestion.
   */
  @JsonProperty("message")
  public String getMessage() {
    return message;
  }

  /**
   * @return The closed-set reason. See <code>PlanLimitReason</code> in
   * <code>channels/types.rs</code> for the full list.
   */
  @JsonProperty("reason")
  public String getReason() {
    return reason;
  }

  /**
   * @return The next tier up the ladder that lifts this axis, or <code>null</code>
   * when the customer is already on business.
   */
  @JsonIgnore
  public Optional<String> getUpgradeTier() {
    if (upgradeTier == null) {
      return Optional.empty();
    }
    return upgradeTier;
  }

  @JsonInclude(
      value = JsonInclude.Include.CUSTOM,
      valueFilter = NullableNonemptyFilter.class
  )
  @JsonProperty("limit")
  private Optional<Integer> _getLimit() {
    return limit;
  }

  @JsonInclude(
      value = JsonInclude.Include.CUSTOM,
      valueFilter = NullableNonemptyFilter.class
  )
  @JsonProperty("upgradeTier")
  private Optional<String> _getUpgradeTier() {
    return upgradeTier;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof PlanLimitErrorBody && equalTo((PlanLimitErrorBody) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(PlanLimitErrorBody other) {
    return code.equals(other.code) && current == other.current && limit.equals(other.limit) && message.equals(other.message) && reason.equals(other.reason) && upgradeTier.equals(other.upgradeTier);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.code, this.current, this.limit, this.message, this.reason, this.upgradeTier);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static CodeStage builder() {
    return new Builder();
  }

  public interface CodeStage {
    /**
     * <p>Always <code>&quot;PLAN_LIMIT&quot;</code>.</p>
     */
    CurrentStage code(@NotNull String code);

    Builder from(PlanLimitErrorBody other);
  }

  public interface CurrentStage {
    /**
     * <p>Current count for the axis that triggered the limit.</p>
     */
    MessageStage current(int current);
  }

  public interface MessageStage {
    /**
     * <p>Human-readable message. Includes the current/limit numbers
     * and the upgrade suggestion.</p>
     */
    ReasonStage message(@NotNull String message);
  }

  public interface ReasonStage {
    /**
     * <p>The closed-set reason. See <code>PlanLimitReason</code> in
     * <code>channels/types.rs</code> for the full list.</p>
     */
    _FinalStage reason(@NotNull String reason);
  }

  public interface _FinalStage {
    PlanLimitErrorBody build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);

    /**
     * <p>The cap value, or <code>null</code> for unlimited (e.g. business tier
     * connection cap).</p>
     */
    _FinalStage limit(Optional<Integer> limit);

    _FinalStage limit(Integer limit);

    _FinalStage limit(Nullable<Integer> limit);

    /**
     * <p>The next tier up the ladder that lifts this axis, or <code>null</code>
     * when the customer is already on business.</p>
     */
    _FinalStage upgradeTier(Optional<String> upgradeTier);

    _FinalStage upgradeTier(String upgradeTier);

    _FinalStage upgradeTier(Nullable<String> upgradeTier);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements CodeStage, CurrentStage, MessageStage, ReasonStage, _FinalStage {
    private String code;

    private int current;

    private String message;

    private String reason;

    private Optional<String> upgradeTier = Optional.empty();

    private Optional<Integer> limit = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(PlanLimitErrorBody other) {
      code(other.getCode());
      current(other.getCurrent());
      limit(other.getLimit());
      message(other.getMessage());
      reason(other.getReason());
      upgradeTier(other.getUpgradeTier());
      return this;
    }

    /**
     * <p>Always <code>&quot;PLAN_LIMIT&quot;</code>.</p>
     * <p>Always <code>&quot;PLAN_LIMIT&quot;</code>.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("code")
    public CurrentStage code(@NotNull String code) {
      this.code = Objects.requireNonNull(code, "code must not be null");
      return this;
    }

    /**
     * <p>Current count for the axis that triggered the limit.</p>
     * <p>Current count for the axis that triggered the limit.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("current")
    public MessageStage current(int current) {
      this.current = current;
      return this;
    }

    /**
     * <p>Human-readable message. Includes the current/limit numbers
     * and the upgrade suggestion.</p>
     * <p>Human-readable message. Includes the current/limit numbers
     * and the upgrade suggestion.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("message")
    public ReasonStage message(@NotNull String message) {
      this.message = Objects.requireNonNull(message, "message must not be null");
      return this;
    }

    /**
     * <p>The closed-set reason. See <code>PlanLimitReason</code> in
     * <code>channels/types.rs</code> for the full list.</p>
     * <p>The closed-set reason. See <code>PlanLimitReason</code> in
     * <code>channels/types.rs</code> for the full list.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("reason")
    public _FinalStage reason(@NotNull String reason) {
      this.reason = Objects.requireNonNull(reason, "reason must not be null");
      return this;
    }

    /**
     * <p>The next tier up the ladder that lifts this axis, or <code>null</code>
     * when the customer is already on business.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage upgradeTier(Nullable<String> upgradeTier) {
      if (upgradeTier.isNull()) {
        this.upgradeTier = null;
      }
      else if (upgradeTier.isEmpty()) {
        this.upgradeTier = Optional.empty();
      }
      else {
        this.upgradeTier = Optional.of(upgradeTier.get());
      }
      return this;
    }

    /**
     * <p>The next tier up the ladder that lifts this axis, or <code>null</code>
     * when the customer is already on business.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage upgradeTier(String upgradeTier) {
      this.upgradeTier = Optional.ofNullable(upgradeTier);
      return this;
    }

    /**
     * <p>The next tier up the ladder that lifts this axis, or <code>null</code>
     * when the customer is already on business.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "upgradeTier",
        nulls = Nulls.SKIP
    )
    public _FinalStage upgradeTier(Optional<String> upgradeTier) {
      this.upgradeTier = upgradeTier;
      return this;
    }

    /**
     * <p>The cap value, or <code>null</code> for unlimited (e.g. business tier
     * connection cap).</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage limit(Nullable<Integer> limit) {
      if (limit.isNull()) {
        this.limit = null;
      }
      else if (limit.isEmpty()) {
        this.limit = Optional.empty();
      }
      else {
        this.limit = Optional.of(limit.get());
      }
      return this;
    }

    /**
     * <p>The cap value, or <code>null</code> for unlimited (e.g. business tier
     * connection cap).</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage limit(Integer limit) {
      this.limit = Optional.ofNullable(limit);
      return this;
    }

    /**
     * <p>The cap value, or <code>null</code> for unlimited (e.g. business tier
     * connection cap).</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "limit",
        nulls = Nulls.SKIP
    )
    public _FinalStage limit(Optional<Integer> limit) {
      this.limit = limit;
      return this;
    }

    @java.lang.Override
    public PlanLimitErrorBody build() {
      return new PlanLimitErrorBody(code, current, limit, message, reason, upgradeTier, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
