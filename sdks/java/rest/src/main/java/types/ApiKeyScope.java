/**
 * This file was auto-generated by Fern from our API Definition.
 */

package dev.brume.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Object;
import java.lang.String;

public final class ApiKeyScope {
  public static final ApiKeyScope MANAGE_KEYS = new ApiKeyScope(Value.MANAGE_KEYS, "manage_keys");

  public static final ApiKeyScope PUBLISH = new ApiKeyScope(Value.PUBLISH, "publish");

  public static final ApiKeyScope READ_STATS = new ApiKeyScope(Value.READ_STATS, "read_stats");

  public static final ApiKeyScope MANAGE_PROJECT = new ApiKeyScope(Value.MANAGE_PROJECT, "manage_project");

  private final Value value;

  private final String string;

  ApiKeyScope(Value value, String string) {
    this.value = value;
    this.string = string;
  }

  public Value getEnumValue() {
    return value;
  }

  @java.lang.Override
  @JsonValue
  public String toString() {
    return this.string;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    return (this == other) 
      || (other instanceof ApiKeyScope && this.string.equals(((ApiKeyScope) other).string));
  }

  @java.lang.Override
  public int hashCode() {
    return this.string.hashCode();
  }

  public <T> T visit(Visitor<T> visitor) {
    switch (value) {
      case MANAGE_KEYS:
        return visitor.visitManageKeys();
      case PUBLISH:
        return visitor.visitPublish();
      case READ_STATS:
        return visitor.visitReadStats();
      case MANAGE_PROJECT:
        return visitor.visitManageProject();
      case UNKNOWN:
      default:
        return visitor.visitUnknown(string);
    }
  }

  @JsonCreator(
      mode = JsonCreator.Mode.DELEGATING
  )
  public static ApiKeyScope valueOf(String value) {
    switch (value) {
      case "manage_keys":
        return MANAGE_KEYS;
      case "publish":
        return PUBLISH;
      case "read_stats":
        return READ_STATS;
      case "manage_project":
        return MANAGE_PROJECT;
      default:
        return new ApiKeyScope(Value.UNKNOWN, value);
    }
  }

  public enum Value {
    PUBLISH,

    READ_STATS,

    MANAGE_KEYS,

    MANAGE_PROJECT,

    UNKNOWN
  }

  public interface Visitor<T> {
    T visitPublish();

    T visitReadStats();

    T visitManageKeys();

    T visitManageProject();

    T visitUnknown(String unknownType);
  }
}
