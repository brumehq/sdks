/**
 * This file was auto-generated by Fern from our API Definition.
 */

package dev.brume;

import dev.brume.core.ClientOptions;
import dev.brume.core.RequestOptions;
import dev.brume.types.ChannelListResponse;
import dev.brume.types.ConnectRequest;
import dev.brume.types.PresenceResponse;
import dev.brume.types.PublishRequest;
import dev.brume.types.PublishResponse;
import java.lang.String;

public class ChannelsClient {
  protected final ClientOptions clientOptions;

  private final RawChannelsClient rawClient;

  public ChannelsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new RawChannelsClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public RawChannelsClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Returns a list of all channels for the authenticated project.
   */
  public ChannelListResponse listChannels() {
    return this.rawClient.listChannels().body();
  }

  /**
   * Returns a list of all channels for the authenticated project.
   */
  public ChannelListResponse listChannels(RequestOptions requestOptions) {
    return this.rawClient.listChannels(requestOptions).body();
  }

  /**
   * Returns the current presence roster for a channel.
   */
  public PresenceResponse getPresence(String channel) {
    return this.rawClient.getPresence(channel).body();
  }

  /**
   * Returns the current presence roster for a channel.
   */
  public PresenceResponse getPresence(String channel, RequestOptions requestOptions) {
    return this.rawClient.getPresence(channel, requestOptions).body();
  }

  /**
   * Server-side REST publish endpoint for non-WebSocket backends
   * (cron jobs, webhooks, queue workers).
   */
  public PublishResponse publish(String channel, PublishRequest request) {
    return this.rawClient.publish(channel, request).body();
  }

  /**
   * Server-side REST publish endpoint for non-WebSocket backends
   * (cron jobs, webhooks, queue workers).
   */
  public PublishResponse publish(String channel, PublishRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.publish(channel, request, requestOptions).body();
  }

  /**
   * JWT extraction priority:
   * <ol>
   * <li><code>Sec-WebSocket-Protocol: brume.token.&lt;jwt&gt;</code> (recommended — keeps the
   * token out of access logs, browser history, and referer headers).</li>
   * <li><code>?token=&lt;jwt&gt;</code> query parameter (legacy; emits a deprecation warning).</li>
   * </ol>
   * <p>If auth fails, returns an HTTP error response without upgrading.</p>
   */
  public void connect() {
    this.rawClient.connect().body();
  }

  /**
   * JWT extraction priority:
   * <ol>
   * <li><code>Sec-WebSocket-Protocol: brume.token.&lt;jwt&gt;</code> (recommended — keeps the
   * token out of access logs, browser history, and referer headers).</li>
   * <li><code>?token=&lt;jwt&gt;</code> query parameter (legacy; emits a deprecation warning).</li>
   * </ol>
   * <p>If auth fails, returns an HTTP error response without upgrading.</p>
   */
  public void connect(RequestOptions requestOptions) {
    this.rawClient.connect(requestOptions).body();
  }

  /**
   * JWT extraction priority:
   * <ol>
   * <li><code>Sec-WebSocket-Protocol: brume.token.&lt;jwt&gt;</code> (recommended — keeps the
   * token out of access logs, browser history, and referer headers).</li>
   * <li><code>?token=&lt;jwt&gt;</code> query parameter (legacy; emits a deprecation warning).</li>
   * </ol>
   * <p>If auth fails, returns an HTTP error response without upgrading.</p>
   */
  public void connect(ConnectRequest request) {
    this.rawClient.connect(request).body();
  }

  /**
   * JWT extraction priority:
   * <ol>
   * <li><code>Sec-WebSocket-Protocol: brume.token.&lt;jwt&gt;</code> (recommended — keeps the
   * token out of access logs, browser history, and referer headers).</li>
   * <li><code>?token=&lt;jwt&gt;</code> query parameter (legacy; emits a deprecation warning).</li>
   * </ol>
   * <p>If auth fails, returns an HTTP error response without upgrading.</p>
   */
  public void connect(ConnectRequest request, RequestOptions requestOptions) {
    this.rawClient.connect(request, requestOptions).body();
  }
}
