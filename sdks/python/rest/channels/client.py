# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.channel_list_response import ChannelListResponse
from ..types.presence_response import PresenceResponse
from ..types.publish_response import PublishResponse
from .raw_client import AsyncRawChannelsClient, RawChannelsClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ChannelsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawChannelsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawChannelsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawChannelsClient
        """
        return self._raw_client

    def list_channels(self, *, request_options: typing.Optional[RequestOptions] = None) -> ChannelListResponse:
        """
        Returns a list of all channels for the authenticated project.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChannelListResponse
            List of channels for the project

        Examples
        --------
        from brume import BrumeClient

        client = BrumeClient(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )
        client.channels.list_channels()
        """
        _response = self._raw_client.list_channels(request_options=request_options)
        return _response.data

    def get_presence(
        self, channel: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PresenceResponse:
        """
        Returns the current presence roster for a channel.

        Parameters
        ----------
        channel : str
            Channel name. Same rules as publish.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PresenceResponse
            Presence roster for the channel

        Examples
        --------
        from brume import BrumeClient

        client = BrumeClient(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )
        client.channels.get_presence(
            channel="channel",
        )
        """
        _response = self._raw_client.get_presence(channel, request_options=request_options)
        return _response.data

    def publish(
        self,
        channel: str,
        *,
        event: str,
        payload: typing.Dict[str, typing.Any],
        ref: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublishResponse:
        """
        Server-side REST publish endpoint for non-WebSocket backends
        (cron jobs, webhooks, queue workers).

        Parameters
        ----------
        channel : str
            Channel name. Must contain at least one `:` separator (e.g., `room:123`). 1-256 chars. Allowed: alphanumeric, `:`, `-`, `_`.

        event : str
            Event name. 1-256 chars. Allowed: alphanumeric, `:`, `-`, `_`, `.`.
            Must NOT start with `brume:` (reserved for system events).

        payload : typing.Dict[str, typing.Any]
            Free-form JSON payload delivered to subscribers.

        ref : typing.Optional[str]
            Optional client-generated idempotency key. Echoed in `brume:ack`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublishResponse
            Message published

        Examples
        --------
        from brume import BrumeClient

        client = BrumeClient(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )
        client.channels.publish(
            channel="channel",
            event="event",
            payload={"key": "value"},
        )
        """
        _response = self._raw_client.publish(
            channel, event=event, payload=payload, ref=ref, request_options=request_options
        )
        return _response.data

    def connect(
        self, *, token: typing.Optional[str] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        JWT extraction priority:
        1. `Sec-WebSocket-Protocol: brume.token.<jwt>` (recommended — keeps the
           token out of access logs, browser history, and referer headers).
        2. `?token=<jwt>` query parameter (legacy; emits a deprecation warning).

        If auth fails, returns an HTTP error response without upgrading.

        Parameters
        ----------
        token : typing.Optional[str]
            Legacy JWT query parameter. Prefer the `Sec-WebSocket-Protocol: brume.token.<jwt>` subprotocol, which keeps the token out of access logs, browser history, and the `Referer` header.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from brume import BrumeClient

        client = BrumeClient(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )
        client.channels.connect()
        """
        _response = self._raw_client.connect(token=token, request_options=request_options)
        return _response.data


class AsyncChannelsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawChannelsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawChannelsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawChannelsClient
        """
        return self._raw_client

    async def list_channels(self, *, request_options: typing.Optional[RequestOptions] = None) -> ChannelListResponse:
        """
        Returns a list of all channels for the authenticated project.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChannelListResponse
            List of channels for the project

        Examples
        --------
        import asyncio

        from brume import AsyncBrumeClient

        client = AsyncBrumeClient(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.channels.list_channels()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_channels(request_options=request_options)
        return _response.data

    async def get_presence(
        self, channel: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PresenceResponse:
        """
        Returns the current presence roster for a channel.

        Parameters
        ----------
        channel : str
            Channel name. Same rules as publish.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PresenceResponse
            Presence roster for the channel

        Examples
        --------
        import asyncio

        from brume import AsyncBrumeClient

        client = AsyncBrumeClient(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.channels.get_presence(
                channel="channel",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_presence(channel, request_options=request_options)
        return _response.data

    async def publish(
        self,
        channel: str,
        *,
        event: str,
        payload: typing.Dict[str, typing.Any],
        ref: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublishResponse:
        """
        Server-side REST publish endpoint for non-WebSocket backends
        (cron jobs, webhooks, queue workers).

        Parameters
        ----------
        channel : str
            Channel name. Must contain at least one `:` separator (e.g., `room:123`). 1-256 chars. Allowed: alphanumeric, `:`, `-`, `_`.

        event : str
            Event name. 1-256 chars. Allowed: alphanumeric, `:`, `-`, `_`, `.`.
            Must NOT start with `brume:` (reserved for system events).

        payload : typing.Dict[str, typing.Any]
            Free-form JSON payload delivered to subscribers.

        ref : typing.Optional[str]
            Optional client-generated idempotency key. Echoed in `brume:ack`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublishResponse
            Message published

        Examples
        --------
        import asyncio

        from brume import AsyncBrumeClient

        client = AsyncBrumeClient(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.channels.publish(
                channel="channel",
                event="event",
                payload={"key": "value"},
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.publish(
            channel, event=event, payload=payload, ref=ref, request_options=request_options
        )
        return _response.data

    async def connect(
        self, *, token: typing.Optional[str] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        JWT extraction priority:
        1. `Sec-WebSocket-Protocol: brume.token.<jwt>` (recommended — keeps the
           token out of access logs, browser history, and referer headers).
        2. `?token=<jwt>` query parameter (legacy; emits a deprecation warning).

        If auth fails, returns an HTTP error response without upgrading.

        Parameters
        ----------
        token : typing.Optional[str]
            Legacy JWT query parameter. Prefer the `Sec-WebSocket-Protocol: brume.token.<jwt>` subprotocol, which keeps the token out of access logs, browser history, and the `Referer` header.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from brume import AsyncBrumeClient

        client = AsyncBrumeClient(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.channels.connect()


        asyncio.run(main())
        """
        _response = await self._raw_client.connect(token=token, request_options=request_options)
        return _response.data
